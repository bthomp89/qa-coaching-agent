# OpenAI API Configuration
# Required: Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
# Optional: Port for the server (defaults to 3001)
PORT=3001

# CORS Configuration
# Optional: Frontend URL for CORS (defaults to http://localhost:5173 for local dev)
# For production, set this to your frontend Render URL (e.g., https://qa-coaching-agent-frontend.onrender.com)
CLIENT_URL=http://localhost:5173

# Node Environment
# Optional: Set to 'production' in production environments
NODE_ENV=development
